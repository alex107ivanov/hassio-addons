{
  "name": "Openvpn Client",
  "version": "0.0.4",
  "slug": "openvpn-client",
  "description": "Client to connect HA to OpenVpn server.",
  "startup": "before",
  "arch": [
    "amd64",
    "armhf",
    "i386",
    "armv7",
    "aarch64"
  ],
  "boot": "auto",
  "privileged": ["NET_ADMIN"],
  "options": {
    "config": [
      "openvpn", 
      "config", 
      "with", 
      "keys"
    ]
  },
  "schema": {
    "config": ["str"]
  }
}
